import { HeroForm } from '@/components/form';

export const metadata = {
  title: 'Next',
};

const Home = () => {
  return (
    <section>
      <h1>next-equities</h1>
      <HeroForm />
    </section>
  );
};

export default Home;
